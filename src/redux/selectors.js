export const userInfoSelector = (state) => state.user;

export const loadingSelector = (state) => state.loading;

export const openChatsSelector = (state) => state.chat.openChats;

export const notificationsMessengerSelector = (state) => state.notifications.messenger;

export const notificationsOtherSelector = (state) => state.notifications.other;
